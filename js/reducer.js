export default function reducer(state, action) {
    switch (action.type){
        case 'SET_DATA' : {
            const r2Data = action.value
            return (
                <Text />
            )
        }
    }
}